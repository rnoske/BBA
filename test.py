

class Data():
    """
    Requirements:
        -needs to handle images (2d) and 2d spectrum like files
        - needs to have some sort of "name"
        - needs an attribute (dictionary?) to hold attributes
        - should be able to create itself (tree-like structure?)
    """
    def __init__(self, name):
        """ Initialisation method
        
        """
        self.name = name
        se